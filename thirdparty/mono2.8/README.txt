The mono.lib file contained in the lib directory was created from the mono.def file that accompanies it.

The headers found in include come from the Mono 2.8 release.

Command to generate:
lib /nologo /def:mono.def /out:mono.lib /machine:x86
