#ifndef CONVERSION_VERSION_H
#define CONVERSION_VERSION_H

/**
  Increment this number if you modify the file formats in a way that breaks backwards compatibility.

  The game will then force the user to increment the number as well.
  */
const static int DataFormatVersion = 1;

#endif // CONVERSION_VERSION_H
